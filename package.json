{
  "type": "module",
  "scripts": {
    "test": "jest --coverage",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "coveralls": "^3.1.1"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/.internal/"
    ]
  }
}